# Frontend

The current dependency-free Arabic RTL frontend is served from the project root:

- `index.html`
- `styles.css`
- `app.js`

This directory is reserved for the planned Next.js migration described in the root README.
