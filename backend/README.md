# Backend

The current lightweight development server is `server.js` in the project root.

The production API layer, background document-processing workers, authentication,
storage integration, and PostgreSQL access will live in this directory.
